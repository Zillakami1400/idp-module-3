# ======================================================
# IDP System — Python Requirements
# ======================================================

# --- Web framework ---
fastapi
uvicorn[standard]
python-multipart          # required for UploadFile / Form in FastAPI

# --- OCR & Extraction (Docling) ---
docling                   # Unified Document extraction via AI and layout analysis
onnxruntime               # Required by docling's RapidOcrOptions

# --- NLP (Module 3 & 4) ---
spacy                     # NLP pipeline for entity extraction, POS tagging
nltk                      # Tokenisation, stopwords, stemming

# --- Embeddings (Module 5) ---
sentence-transformers     # all-MiniLM-L6-v2 embedding model

# --- Vector search (Module 5) ---
faiss-cpu                 # FAISS vector index (CPU build)

# --- Data utilities ---
numpy
pandas




# --- RAG / LangChain (Module 6) ---
langchain                 # core LangChain orchestration
langchain-community       # FAISS vectorstore + community integrations
langchain-huggingface     # HuggingFaceEmbeddings wrapper (reuses sentence-transformers)
langchain-ollama          # ChatOllama LLM wrapper for local Ollama

# --- Testing ---
httpx                     # async HTTP client used by FastAPI TestClient
pytest